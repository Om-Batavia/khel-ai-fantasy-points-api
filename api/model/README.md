# Packaged Model

These files are copied from the verified offline training output:

- `model.pkl`: fitted `StandardScaler + LinearRegression` pipeline
- `feature_columns.json`: exact ordered API feature contract
- `metrics.json`: chronological evaluation results
- `sample_input.json`: valid single prediction input

The model is demo-only because its source dataset is synthetic/faux.
